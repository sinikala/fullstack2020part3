{
  "name": "puhelinluettelobackend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf build && cd ../fullstack2020/osa2/puhelinluettelo && npm run build --prod && cp -r build ../../../fullstack2020part3/",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",
    "test": "echo \"Error: no test specified\" && exit 1",
    "logs:prod": "heroku logs --tail"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sinikala/fullstack2020part3.git"
  },
  "author": "sinikala",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/sinikala/fullstack2020part3/issues"
  },
  "homepage": "https://github.com/sinikala/fullstack2020part3#readme",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.8.11",
    "morgan": "^1.9.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
